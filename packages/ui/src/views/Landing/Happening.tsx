import React from 'react';
import styled from 'styled-components';
import { LandingTop } from '../../components/LandingTop';

const HappeningWrapper = styled.section``;

export const HappeningView: React.FC = () => {
  return (
    <HappeningWrapper className="landing-grid">
      <LandingTop title="👀 Happening"></LandingTop>
      happening
    </HappeningWrapper>
  );
};
